
eaa-kit audit 5 pages · jsdom (browserless)

Issues
  3 distinct elements to fix.

  ✗ image-alt critical, WCAG 1.1.1
      Images must have alternative text
      A screen reader announces this image by its filename, or skips it
      entirely.
      Fix: Astro's <Image /> requires alt. Give it one describing what the
      image conveys, or alt="" where it repeats adjacent text.
      → <img src="/assets/logo.svg" alt="What this image shows">
      <img src="/assets/logo.svg">
        on 1 page:
          index.html

  ✗ html-has-lang serious, WCAG 3.1.1
      <html> element must have a lang attribute
      A screen reader reads the page in whatever language it defaults to, so
      German content is read with English pronunciation rules and is close to
      unintelligible.
      Fix: Set lang on the <html> element in your layout under src/layouts,
      not in the individual page.
      → <html lang="de">
      <html><head> <title>Startseite</title> </head> <body> <img src="/assets/l…
        on 1 page:
          index.html

  ✗ link-name serious, WCAG 2.4.4 4.1.2
      Links must have discernible text
      A screen reader announces this as "link" with nothing after it, so where
      it goes is unknowable without following it.
      Fix: Give the link text. Where the design calls for an icon alone, keep
      the visible icon and add a visually hidden label or an aria-label.
      → <a href="/about/">Where this link goes</a>
      <a href="/about/"></a>
        on 1 page:
          index.html
Summary
  3 violations on 1 of 5 pages (3 elements)
  3 at or above serious (--fail-on serious)
  1 rule needs manual review

Not evaluated
  This engine reached no verdict on these.
  They are never reported as passing.
  · color-contrast 5 pages, WCAG 1.4.3
      needs rendered foreground and background colours
      1.4.3: https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html
  · link-in-text-block 5 pages, WCAG 1.4.1
      needs rendered colours and text decoration
      1.4.1: https://www.w3.org/WAI/WCAG22/Understanding/use-of-color.html
  · no-autoplay-audio 5 pages, WCAG 1.4.2
      needs media duration, and media is never loaded
      1.4.2: https://www.w3.org/WAI/WCAG22/Understanding/audio-control.html
  · scrollable-region-focusable 5 pages, WCAG 2.1.1 2.1.3
      needs computed overflow
      2.1.1: https://www.w3.org/WAI/WCAG22/Understanding/keyboard.html
  · target-size 1 page, WCAG 2.5.8
      needs element geometry; every box is 0x0 without layout
      2.5.8: https://www.w3.org/WAI/WCAG22/Understanding/target-size-minimum.ht…
  Run with --manual for what to check by hand.

  Of the 55 WCAG 2.2 A and AA success criteria, 34 cannot be checked by any
  automated engine and need a person. This run reached a verdict on 6.
  --browser would answer 5 more criteria.
  Run with --coverage for the criterion-by-criterion breakdown.
